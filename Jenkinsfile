pipeline {
   agent any
     stages{
       stage ('Build'){
      steps {  echo'Building' }
         }
      stage ('Test'){
        steps { echo 'testing'}
        }
      stage ('Done'){
       steps {echo 'Program executed'}
         }
   }
}
