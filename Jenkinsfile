pipeline {
   agent any
     stages{
       stage ('Build'){
      steps {  echo'Building' }
         }
      stage ('Test'){
        steps { echo 'testing'}
        }
      stages ('Done'){
       step {echo 'Program executed'}
         }
   }
}
