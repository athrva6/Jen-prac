pipeline {
   agent any
     stages{
       stage ('Build'){
      step {  echo'Building' }
         }
      stage ('Test'){
        step { echo 'testing'}
        }
      stage ('Done'){
       step {echo 'Program executed'}
         }
   }
}
